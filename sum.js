// Take input from the user
let a = parseInt(prompt("Enter first digit (a):"));
let b = parseInt(prompt("Enter second digit (b):"));

// Calculate the sum
let x = a + b;

// Print the result
console.log("X = " + x);
